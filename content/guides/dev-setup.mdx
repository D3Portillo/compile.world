---
sidebarTitle: Initial Setup
---

# What is Worldchain?

Hello, world!

```js {1,4-5} filename="example.js"
import { useState } from "react"

function Counter() {
  const [count, setCount] = useState(0)
  return <button onClick={() => setCount(count + 1)}>{count}</button>
}
```

## Install

```sh npm2yarn
npm i -D @graphql-eslint/eslint-plugin
```
